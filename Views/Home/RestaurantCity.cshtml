
@{
    ViewData["Title"] = "مدن المطاعم";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="content">
    <div class="content dark-edition">
        <div class="container-fluid">

            <div class="container alignright" style="margin:2% 0;">
                <div class="row">
                    <div class="col-lg-6 col-md-6 col-sm-12 " style="text-align: center;">
                        <label class="mb-2">اسم المطعم</label>
                        <input type="text" class="form-control alignright" placeholder="اكتب اسم المطعم" name="Name" id="Name">
                    </div>
                    <div class="col-lg-2 col-md-2 col-sm-2 col-xs-2" style="text-align: center;">
                        <div class="text-center wow fadeInDown" style="text-align:left;">
                            <label class="mb-2"> </label>
                            <button type="button" id="search-btn" class="btn btn-info btnwidth" style="text-align:center; width:100%;">بحث</button>
                        </div>
                    </div>
                    <div class="col-lg-2 col-md-2 col-sm-2 col-xs-2">
                        <div class="text-center wow fadeInDown">
                            <label class="mb-2"> </label>
                            <button type="button" class="btn btn-success btnwidth" style="text-align:center; width:100%;" data-toggle="modal" data-target="#RestaurantCityModal">اضافة  جديد <i class='material-icons'>add</i> </button>
                        </div>
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <div class="card">
                        <div class="card-header card-header-primary" style="text-align:right">
                            <h4 class="card-title ">مدن المطاعم</h4>
                            <p class="card-category">جدول لعرض مدن المطاعم داخل التطبيق</p>
                        </div>
                        <div class="card-body">
                            <div class="table-responsive" style="text-align: right;">
                                <table class="table table-hover">
                                    <thead class=" ">

                                        <tr>
                                            <th scope="col">كلفة التوصيل</th>
                                            <th scope="col">اسم المدينة</th>
                                            <th scope="col">اسم المطعم</th> 
                                            <th scope="col"></th>
                                        </tr>
                                    </thead>
                                    <tbody id="tableRestaurantCity">
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modal -->
<div class="modal fade" id="RestaurantCityModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content dark-edition">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">اضافة جديد</h5>
                <button type="button" id="close1" style="margin-left:10%;" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true" style="font-size:155%">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="row"> 
                    <div class="form-group col-md-12  alignright">
                        <label class="text-primary">اسم المدينة</label>
                        <select class="form-control text-right" style="background-color: #1a2035;" name="CityId" id="CityId">
                        </select>
                    </div>
                    <div class="form-group col-md-12  alignright">
                        <label class="text-primary">اسم المطعم</label>
                        <select class="form-control text-right" style="background-color: #1a2035;" name="RestaurantId" id="RestaurantId">
                        </select>
                    </div>
                </div>

                <div class="row col-md-6 alignright">
                    <label class="text-primary"> كلفة التوصيل</label>
                    <input type="text" placeholder="اكتب   كلفة التوصيل " class="form-control alignright" autocomplete="off" name="CostDelivery" id="CostDelivery" />
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" id="close" data-dismiss="modal">اغلاق</button>
                <button type="button" class="btn btn-success" id="save">حفظ</button>
            </div>
        </div>
    </div>
</div>
<div id="mydiv">
    <script>

        function selectRestaurantId(data) {
            var s = '<option value="0">اختيار المطعم</option>';
            $('#RestaurantId').append(s);
            $.each(data, function (i, item) {
                var ss = '<option value="' + item.restaurantId + '">' + item.name + '</option>';
                $('#RestaurantId').append(ss);
            });
        };
        function selectCityId(data) {
            var s = '<option value="0">اختيار المدينة</option>';
            $('#CityId').append(s);
            $.each(data, function (i, item) {
                var ss = '<option value="' + item.cityId + '">' + item.cityName + '</option>'; 
                $('#CityId').append(ss);
            });
        };
        $("#close1").on("click", (e) => {
            e.preventDefault();
            aftersaveRestaurantCity();
        });
        $("#close").on("click", (e) => {
            e.preventDefault();
            aftersaveRestaurantCity();
        });
        $("#search-btn").on("click", (e) => {
            RefreshRestaurantCity();
        });

        $(document).ready(function () { 
            var obj={Name:''}
            call_ajax("GET", "City/GetAll", obj, selectCityId);
            call_ajax("GET", "Restaurant/GetAll", obj, selectRestaurantId);
            RefreshRestaurantCity();
        });
        $("#save").on("click", (e) => {
            e.preventDefault();  
            if ($("#CityId").val() === "0" || $("#CityId").val().trim() === "") {
                md.showNotification("رجاءا ادخل اسم المدينة ");
                return;
            }  
            if ($("#RestaurantId").val() === "0" || $("#RestaurantId").val().trim() === "") {
                md.showNotification("رجاءا ادخل اسم المطعم ");
                return;
            } 
            var data = new FormData();
            data.append('RestaurantCityId', _RestaurantCityId);
            data.append('RestaurantId', $("#RestaurantId").val());
            data.append('CityId', $("#CityId").val());
            data.append('CostDelivery', $("#CostDelivery").val());
            call_ajax_withfile("POST", "RestaurantCity/Post", data, aftersaveRestaurantCity); 
        });

    </script>
</div>




